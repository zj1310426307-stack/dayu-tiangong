# QGIS Server uses this service definition plus a runtime-only secret from Compose.
# No credential value, host path, or desktop profile identifier is stored here.
[dayu_qgis_server]
host=database
port=5432
dbname=dayu_tiangong
user=dayu_qgis_server
sslmode=prefer
connect_timeout=5
